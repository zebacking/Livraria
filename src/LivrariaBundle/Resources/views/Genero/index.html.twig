{% extends 'base.html.twig' %}

{% block body %}
    <h1>Lista de Gêneros</h1>
    <a href="{{ path('genero_new') }}"class="btn btn-primary pull-right"><span class="glyphicon glyphicon-plus"></span> Criar Novo Genero</a>

    <table class="table table-hover">
        <thead>
            <tr>
                <!--<th>Id</th>-->
                <th>Nome</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            {% for genero in generos %}
                <tr>
                  <!--  <td><a href="{{ path('genero_show', { 'id': genero.id }) }}">{{ genero.id }}</a></td>-->
                    <td>{{ genero.nome }}</td>
                    <td>
                        <a href="{{ path('genero_show', { 'id': genero.id }) }}" class="btn btn-default btn-sm" title="Mostra Genero">
                        <span class="glyphicon glyphicon-eye-open"></span></a>
                        <a href="{{ path('genero_edit', { 'id': genero.id }) }}"class="btn btn-warning btn-sm" title="Edita Genero">
                        <span class="glyphicon glyphicon-pencil"></span></a>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>




{% endblock %}
